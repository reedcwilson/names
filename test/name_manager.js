"use strict";

let assert = require("assert");
let getPopular = require('../name_manager.js');

describe('name_manager', function() {
  describe('#getPopular()', function () {
    describe('range', function() {
      it('should ensure that the range is required', function () {
        assert.equal(1, 1);
      });
    });
  });
});

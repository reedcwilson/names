language: node_js

node_js:
  - "4.1"

before_install:
  - "npm install -g bower"
  - "sh ./scripts/install.sh"

before_script:
  - "npm install -g grunt-cli"
  - "npm install -g mocha"
  - "gem install compass"

script: 
  - "sh ./scripts/test.sh"

after_success:
  - "bash ./scripts/deploy.sh"
